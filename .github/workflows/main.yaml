name: main
on:
  push:
    branches: [main]

# Limit the concurrency of the whole main pipeline to prevent
# race conditions in back-to-back workflow runs
concurrency:
  group: deploy_production

jobs:
  ci:
    uses: ./.github/workflows/ci.yaml
    secrets: inherit

  publish-release:
    runs-on: ubuntu-latest
    env:
      REGISTRY: ghcr.io
      IMAGE_NAME: ${{ github.repository }}
      JOB_IMAGE_NAME: ${{ github.repository }}-job
      WEB_SERVICE_ID: 499392e1-8a16-4c4f-88b4-70fbf3ab76cc
      MIGRATE_SERVICE_ID: b86ccf0b-266b-4332-8c09-46771bb230d1
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
    needs: ci
    environment:
      name: production
      url: https://timomeh.de
    steps:
      - uses: actions/checkout@v6.0.2
      - name: install railway cli
        run: bash <(curl -fsSL cli.new)
      - run: source "$HOME/.railway/env"
      - uses: docker/login-action@v4.2.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/download-artifact@v8
        with:
          name: docker-image
          path: /tmp
      - uses: actions/download-artifact@v8
        with:
          name: docker-job-image
          path: /tmp
      - name: load image
        run: docker load -i /tmp/docker-image.tar
      - name: load job image
        run: docker load -i /tmp/docker-job-image.tar
      - name: publish image
        run: docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
      - name: publish job image
        run: docker push ${{ env.REGISTRY }}/${{ env.JOB_IMAGE_NAME }}:latest
      - name: release service
        run: railway redeploy -s ${{ env.WEB_SERVICE_ID }} -y
      - name: migrate database
        run: railway redeploy -s ${{ env.MIGRATE_SERVICE_ID }} -y
